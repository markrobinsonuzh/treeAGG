% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tx_node.R
\name{tx_node}
\alias{tx_node}
\title{transfer between node number and node label}
\usage{
tx_node(tree, input)
}
\arguments{
\item{tree}{a phylo object}

\item{input}{a tree node label or a tree node number}
}
\description{
\code{tx_node} is to do the transfermation between the number and the label of a node on a tree
}
\examples{


library(ggtree)

data(tinyTree)

ggtree(tinyTree, branch.length = 'none') +
geom_text2(aes(label = label), hjust = -0.3) +
geom_text2(aes(label = node), vjust = -0.8,
hjust = -0.3, color = 'blue')

#check whether the node number and node label are matched
tx_node(tinyTree, input = c(11, 2, 4))

tx_node(tinyTree, input = c('Node_16', 'Node_11'))


}
